spring.flyway.enabled=false
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Springdoc
springdoc.enabled=false

# debug logging
debug=false

# Azure
azure.openid-config-token-endpoint=${wiremock.server.baseUrl}/azure_token
azure.app-client-id=test-client
azure.app-client-secret=test-secret

digdir.krr.proxy.baseUrl=${wiremock.server.baseUrl}/digdir_krr_proxy/
digdir.krr.proxy.scope=test-scope

# Postgres
db.url=jdbc:postgresql://b12abcd022.test.local:5432/doknotifikasjon-t
db.credentialsmountpath=postgresql/preprod-fss

autostartup.av.kafkalyttere.for.knot=true

# Slack
doknotifikasjon.slack.token=xoxb-123
doknotifikasjon.slack.channel=test-channel
doknotifikasjon.slack.enabled=false

nais.token-endpoint=${wiremock.server.baseUrl}/nais-texas
altinn3.notification-order-uri=${wiremock.server.baseUrl}/altinn-order-notification
altinn3.altinn-token-exchange-uri=${wiremock.server.baseUrl}/token-exchange
