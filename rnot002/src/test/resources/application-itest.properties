spring.flyway.enabled=false

# Springdoc
springdoc.enabled=false

nais.token-endpoint=http://localhost:${wiremock.server.port}/nais-texas
altinn3.notification-order-uri=http://localhost:${wiremock.server.port}/altinn-order-notification
altinn3.altinn-token-exchange-uri=http://localhost:${wiremock.server.port}/token-exchange

# Azure
azure.openid-config-token-endpoint=http://localhost:${wiremock.server.port}/azure_token
azure.app-client-id=test-client
azure.app-client-secret=test-secret

# Azure-mock
no.nav.security.jwt.issuer.azurev2.discoveryUrl=http://localhost:${mock-oauth2-server.port}/azurev2/.well-known/openid-configuration
no.nav.security.jwt.issuer.azurev2.acceptedAudience=gosys

# DigDir
digdir.krr.proxy.baseUrl=http://localhost:${wiremock.server.port}/digdir_krr_proxy/
digdir.krr.proxy.scope=test-scope

# Postgres
db.url=jdbc:postgresql://b12abcd022.test.local:5432/doknotifikasjon-t
db.credentialsmountpath=postgresql/preprod-fss

# Tid i ms
retry.delay=10
retry.attempts=5

# Slack
doknotifikasjon.slack.token=xoxb-123
doknotifikasjon.slack.channel=test-channel
doknotifikasjon.slack.enabled=false